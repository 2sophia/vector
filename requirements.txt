fastapi
uvicorn[standard]
pydantic
pydantic-settings
cryptography
httpx
aiofiles
qdrant-client
python-multipart
python-jose[cryptography]
passlib[bcrypt]
python-slugify
requests
pymongo
falkordb
croniter
# database tz IANA per zoneinfo (SCHEDULER_TZ): i container slim non hanno
# /usr/share/zoneinfo → senza questo ZoneInfo("Europe/Rome") fallirebbe.
tzdata
# entity extraction (M3 knowledge graph). Per CPU-only installare torch da:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
gliner
# classificazione zero-shot dei chunk (opt-in, CLASSIFIER_ENABLED). Tira dietro
# scikit-learn/scipy: serve solo se accendi il classifier, ma lo teniamo allineato.
gliclass
python-dotenv
rich
# conversione mail Outlook .msg → HTML (pre-parser, vedi utils/convert.py).
# .eml usa la stdlib; l'Office binario/.rtf/.odf passa da LibreOffice (Dockerfile).
extract-msg
# trascrizione audio/video su CPU (lazy, vedi utils/transcribe.py). Richiede
# ffmpeg (Dockerfile) per estrarre l'audio dai video.
faster-whisper
# MCP server (/mcp, MCP_ENABLED): espone search + NLP come tool Model Context Protocol.
mcp
# chunker tabulare (utils/tabular.py): CSV/XLSX grandi → "table card" + righe
# verbalizzate invece di mandarli a Docling (che esplode sulle tabelle enormi).
# pandas per parsing robusto (sniff separatore, decimale IT, dtype); openpyxl per .xlsx.
pandas
openpyxl